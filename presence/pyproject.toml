[project]
name = "agathos-presence"
version = "0.1.0"
description = "Discord Gateway presence companion for Agathos"
requires-python = ">=3.13"
dependencies = [
    "discord.py>=2.6,<3",
]

[tool.uv]
package = false
